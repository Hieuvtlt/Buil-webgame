import './combat.css'
import { player, getPlayerStats, syncDerivedStats, gainExperience } from '../data/character.js'

const QUEST_STATE_KEY = 'game.quest.state.v1'

function loadQuestState(){try{return JSON.parse(localStorage.getItem(QUEST_STATE_KEY))||{monster:[],monsterProgress:{},wanted:null,wantedCount:0}}catch{return{monster:[],monsterProgress:{},wanted:null,wantedCount:0}}}
function saveQuestState(state){localStorage.setItem(QUEST_STATE_KEY,JSON.stringify(state))}
function addLog(message,type='system'){window.dispatchEvent(new CustomEvent('game:log',{detail:{message,type}}))}
function clamp(v,min,max){return Math.max(min,Math.min(max,v))}
function randomInt(min,max){return Math.floor(Math.random()*(max-min+1))+min}
function createMonster(area){const entry=area.monsters[randomInt(0,area.monsters.length-1)];const level=randomInt(Number(entry[1]),Number(entry[2]));const roll=Math.random();const rank=roll<0.02?'goldboss':roll<0.12?'subboss':'normal';const mult=rank==='goldboss'?4:rank==='subboss'?2.2:1;const maxHp=Math.round((80+level*45)*mult);const maxMp=Math.round((40+level*18)*mult);return{name:entry[0],level,maxHp,hp:maxHp,maxMp,mp:maxMp,attack:Math.round((8+level*4)*mult),defense:Math.round((3+level*2)*mult),rank}}

function updateMonsterQuest(mapName,targetName){const state=loadQuestState();state.monster=Array.isArray(state.monster)?state.monster:[];state.monsterProgress=state.monsterProgress&&typeof state.monsterProgress==='object'?state.monsterProgress:{};const questTemplates=[['Rừng Rậm Ven Hồ','Huyết Lang',30],['Bạch Thủy Động','Động Quật Xà',36],['Hắc Hổ Lâm','Hắc Hổ',40],['Thanh Xà Cốc','Thanh Xà',45],['Vũ Lăng Sơn','Hắc Hùng',55],['Dược Vương Cốc','Sơn Tặc Đầu Mục',70],['Tần Lăng','Huyết Sát Ác Tặc',80],['Phù Dung Động','Xích Viêm Ác Tặc',90],['Thiên Sơn Tuyết Cốc','Thiên Sơn Tuyết Yêu',200],['Vạn Độc Cốc','Vạn Độc Ma Tu',240]];const completed=[];for(const index of state.monster){const q=questTemplates[index];if(!q||q[0]!==mapName||q[1]!==targetName)continue;const current=Number(state.monsterProgress[index]||0)+1;state.monsterProgress[index]=Math.min(q[2],current);if(current>=q[2])completed.push(index)}saveQuestState(state);if(completed.length)window.dispatchEvent(new CustomEvent('game:quest-completed',{detail:{type:'monster',indexes:completed}}));window.dispatchEvent(new CustomEvent('game:quest-progress-changed'));return completed.length>0}
function completeWanted(mapName,targetName){const state=loadQuestState();if(state.wanted===null||state.wanted===undefined)return false;const wanted=[['Hắc Phong Đao Khách','Đường Môn Cổ Địa'],['Huyết Ảnh Ma Nhân','Dược Vương Cốc'],['Thiết Diện Quỷ','Lão Hổ Động'],['Bạch Cốt Khách','Phù Dung Động'],['Xích Viêm Cuồng Đồ','Huyết Sa Mạc']];const q=wanted[Number(state.wanted)];if(!q||q[0]!==targetName||q[1]!==mapName)return false;state.wanted=null;saveQuestState(state);window.dispatchEvent(new CustomEvent('game:wanted-completed',{detail:{target:targetName,map:mapName}}));window.dispatchEvent(new CustomEvent('game:quest-progress-changed'));return true}

export function mountCombatOverlay(){
  let root=document.querySelector('#combat-root');if(root)return
  root=document.createElement('div');root.id='combat-root';document.body.appendChild(root)
  let state=null,timer=null,effectTimer=null
  function stopLoop(){if(timer)clearInterval(timer);timer=null}
  function close(){stopLoop();if(effectTimer)clearTimeout(effectTimer);effectTimer=null;root.innerHTML='';state=null;window.dispatchEvent(new CustomEvent('game:combat-closed'))}
  function returnToCharacter(){if(!state||player.hp>0)return;const stats=getPlayerStats();player.hp=stats.maxHp;player.mp=stats.maxMp;syncDerivedStats();window.dispatchEvent(new CustomEvent('game:character-changed'));addLog('Nhân vật đã về thành dưỡng sức.','system');close();document.querySelector('#left-menu .menu-item[data-screen="Nhân vật"]')?.click()}
  function rankLabel(m){return m.rank==='goldboss'?'BOSS HOÀNG KIM':m.rank==='subboss'?'TIỂU BOSS':'QUÁI THƯỜNG'}
  function unitMarkup(kind,e,extra=''){const p=kind==='player',hpPct=clamp(e.hp/e.maxHp*100,0,100),mpPct=clamp(e.mp/e.maxMp*100,0,100),color=p?'white':e.rank==='goldboss'?'gold':e.rank==='subboss'?'blue':'red',name=p?'Nhân vật':e.name,level=p?player.level:e.level;return `<div class="combat-unit-wrap ${kind} ${extra}"><div class="combat-unit-name">${name} · Lv.${level}</div><div class="combat-mini-bars"><div class="combat-mini-bar hp"><span style="width:${hpPct}%"></span></div><div class="combat-mini-bar mp"><span style="width:${mpPct}%"></span></div></div><div class="combat-unit ${color}"><span class="combat-unit-mark">${p?'◆':e.rank==='goldboss'?'★':e.rank==='subboss'?'◆':'●'}</span></div><div class="combat-rank">${p?'NHÂN VẬT':rankLabel(e)}</div></div>`}
  function render(){if(!state)return;const stats=getPlayerStats(),hpPct=clamp(player.hp/stats.maxHp*100,0,100),mpPct=clamp(player.mp/stats.maxMp*100,0,100),dead=player.hp<=0,defeated=state.defeated,effects=state.effects.map(e=>`<span class="damage-float ${e.kind}" style="--fx:${e.x}%;--fy:${e.y}%">${e.text}</span>`).join('');root.innerHTML=`<div class="combat-overlay"><section class="combat-window square-combat-window" role="dialog" aria-modal="true"><header class="combat-header"><div><div class="combat-title">⚔ ${state.area.name}</div><div class="combat-header-meta">Lv.${state.area.min}–${state.area.max} · ${state.area.terrain} · Quái đã hạ: ${state.kills}</div></div><button class="combat-close" type="button" id="combat-close">×</button></header><main class="combat-arena"><section class="combat-player-status"><div class="combat-large-bar hp"><span style="width:${hpPct}%"></span></div><div class="combat-large-label">HP ${Math.max(0,Math.floor(player.hp)).toLocaleString('vi-VN')} / ${stats.maxHp.toLocaleString('vi-VN')}</div><div class="combat-large-bar mp"><span style="width:${mpPct}%"></span></div><div class="combat-large-label">MP ${Math.max(0,Math.floor(player.mp)).toLocaleString('vi-VN')} / ${stats.maxMp.toLocaleString('vi-VN')}</div></section><section class="combat-battlefield ${state.engaged?'engaged':''}"><div class="combat-effects">${effects}</div><div class="combat-unit-stage">${unitMarkup('player',player,state.lastAction==='player'?'attack-player':'')}<div class="combat-distance">⚔</div>${unitMarkup('monster',state.monster,state.lastAction==='monster'?'attack-monster':'')}</div></section><div class="combat-log" id="combat-log">${state.logs.map(x=>`<div class="combat-log-line ${x.type||''}">${x.text}</div>`).join('')}</div><div class="combat-controls"><div class="combat-skill-row">${[1,2,3,4,5].map(slot=>`<button class="combat-skill empty-skill" type="button" data-skill-slot="${slot}" disabled><span class="empty-skill-number">${slot}</span></button>`).join('')}</div><div class="combat-item-row"><button class="combat-item-slot" type="button" title="Đan dược hồi HP" ${dead?'disabled':''}><span class="potion-icon hp-potion"></span><small>HP</small></button><button class="combat-item-slot" type="button" title="Đan dược hồi MP" ${dead?'disabled':''}><span class="potion-icon mp-potion"></span><small>MP</small></button><button class="combat-toggle ${state.auto?'active':''}" type="button" id="combat-auto">${state.auto?'✓ AUTO':'AUTO'}</button>${defeated?`<button class="combat-action" type="button" id="combat-next">TÌM QUÁI TIẾP</button>`:dead?'':`<button class="combat-action danger" type="button" id="combat-leave">RỜI KHU VỰC</button>`}</div>${state.autoMenu?`<div class="combat-auto-menu"><b>CÀI ĐẶT AUTO</b><label><input type="checkbox" data-auto="attack" ${state.autoSettings.attack?'checked':''}/> Tự động đánh thường</label><label><input type="checkbox" data-auto="skill" ${state.autoSettings.skill?'checked':''}/> Tự động dùng kỹ năng</label><label><input type="checkbox" data-auto="hp" ${state.autoSettings.hp?'checked':''}/> Tự dùng đan HP</label><label><input type="checkbox" data-auto="mp" ${state.autoSettings.mp?'checked':''}/> Tự dùng đan MP</label><button type="button" class="combat-auto-close" id="combat-auto-close">ĐÓNG</button></div>`:''}</div></main>${dead?`<div class="combat-defeat-overlay"><button class="combat-defeat-title" type="button" id="combat-return-character">VỀ THÀNH DƯỠNG SỨC</button><div class="combat-defeat-sub">Nhấn vào để trở về menu nhân vật</div></div>`:''}<footer class="combat-footer"><span class="combat-target">🎯 Mục tiêu: ${state.monster?.name||'—'}</span><span>${dead?'<b class="combat-defeat">Nhân vật đã thất bại.</b>':defeated?'<b class="combat-complete">Đã hạ quái.</b>':'Đang giao chiến...'}</span></footer></section></div>`;root.querySelector('#combat-close')?.addEventListener('click',close);root.querySelector('#combat-leave')?.addEventListener('click',close);root.querySelector('#combat-return-character')?.addEventListener('click',returnToCharacter);root.querySelector('#combat-auto')?.addEventListener('click',()=>{state.autoMenu=!state.autoMenu;render()});root.querySelector('#combat-auto-close')?.addEventListener('click',()=>{state.autoMenu=false;render()});root.querySelectorAll('[data-auto]').forEach(el=>el.addEventListener('change',()=>{state.autoSettings[el.dataset.auto]=el.checked}));root.querySelector('#combat-next')?.addEventListener('click',nextMonster);root.querySelectorAll('.combat-item-slot').forEach((btn,index)=>btn.addEventListener('click',()=>useRecoveryItem(index)));const log=root.querySelector('#combat-log');if(log)log.scrollTop=log.scrollHeight}
  function pushEffect(text,kind,target){state.effects.push({text,kind,x:target==='monster'?72:28,y:42+randomInt(-5,5)});if(state.effects.length>8)state.effects.shift();if(effectTimer)clearTimeout(effectTimer);effectTimer=setTimeout(()=>{if(!state)return;state.effects.shift();render()},850)}
  function approach(){state.engaged=true;render()}
  function useRecoveryItem(index){if(!state||player.hp<=0)return;const stats=getPlayerStats();if(index===0){const amount=Math.round(stats.maxHp*.2);player.hp=Math.min(stats.maxHp,player.hp+amount);state.logs.push({text:`💊 Dùng đan hồi HP: +${amount} HP.`,type:'reward'})}else{const amount=Math.round(stats.maxMp*.2);player.mp=Math.min(stats.maxMp,player.mp+amount);state.logs.push({text:`💧 Dùng đan hồi MP: +${amount} MP.`,type:'reward'})}window.dispatchEvent(new CustomEvent('game:character-changed'));render()}
  function playerAttack(){if(!state||state.defeated||player.hp<=0||!state.monster)return;if(!state.engaged){approach();return}const stats=getPlayerStats(),damage=randomInt(stats.attackMin,Math.max(stats.attackMin,stats.attackMax));state.monster.hp=Math.max(0,state.monster.hp-damage);state.lastAction='player';state.logs.push({text:`Bạn gây ${damage} sát thương lên ${state.monster.name}.`,type:'hit'});pushEffect(`-${damage}`,'damage','monster');if(state.logs.length>40)state.logs.shift();if(state.monster.hp<=0){finishMonster();return}const monsterDamage=Math.max(1,state.monster.attack-stats.defense);state.lastAction='monster';player.hp=Math.max(0,player.hp-monsterDamage);state.logs.push({text:`${state.monster.name} gây ${monsterDamage} sát thương.`,type:'damage'});pushEffect(`-${monsterDamage}`,'damage','player');if(player.hp<=0){stopLoop();state.logs.push({text:'Nhân vật đã gục ngã.',type:'damage'});render();return}render()}
  function startLoop(){stopLoop();if(!state?.auto||state.defeated||player.hp<=0)return;timer=setInterval(playerAttack,1350)}
  function finishMonster(){stopLoop();state.defeated=true;state.kills+=1;const exp=10+state.monster.level*4+(state.monster.rank==='goldboss'?Math.round(state.monster.level*8):state.monster.rank==='subboss'?Math.round(state.monster.level*3):0),gold=5+state.monster.level*3+(state.monster.rank==='goldboss'?state.monster.level*8:state.monster.rank==='subboss'?state.monster.level*2:0),oldLevel=player.level;gainExperience(exp);player.gold+=gold;syncDerivedStats();const questDone=updateMonsterQuest(state.area.name,state.monster.name),wantedDone=completeWanted(state.area.name,state.monster.name);state.logs.push({text:`🎁 +${exp} EXP · +${gold} vàng${state.monster.rank!=='normal'?` · ${rankLabel(state.monster)}`:''}`,type:'reward'});if(questDone)state.logs.push({text:'✓ Một nhiệm vụ quái vật đã hoàn thành.',type:'reward'});if(wantedDone)state.logs.push({text:'☠ Truy nã hoàn thành! Nhận thưởng truy nã.',type:'reward'});if(player.level>oldLevel)state.logs.push({text:`★ Nhân vật lên Lv.${player.level}!`,type:'reward'});window.dispatchEvent(new CustomEvent('game:character-changed'));addLog(`Hạ ${state.monster.name} Lv.${state.monster.level}: +${exp} EXP, +${gold} vàng.`,'item');render()}
  function nextMonster(){if(!state)return;state.defeated=false;state.engaged=false;state.lastAction='';state.monster=createMonster(state.area);state.effects=[];state.logs.push({text:`Bạn tiếp tục tìm quái trong ${state.area.name}.`,type:'system'});render();setTimeout(approach,180);startLoop()}
  function start(detail){close();const area=detail?.area;if(!area?.monsters?.length)return;syncDerivedStats();state={area,kills:0,auto:true,autoMenu:false,autoSettings:{attack:true,skill:false,hp:false,mp:false},monster:createMonster(area),defeated:false,engaged:false,lastAction:'',effects:[],logs:[{text:`Đã tiến vào ${area.name}. Mục tiêu đầu tiên: ${area.monsters[0][0]}.`,type:'system'}]};render();setTimeout(approach,180);startLoop()}
  window.addEventListener('game:start-combat',event=>start(event.detail));window.addEventListener('game:close-combat',close)
}
